import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('controller:ita-office-location/index', 'ItaOfficeLocationIndexController', {
  // Specify the other units that are required for this test.
  needs: ['controller:ita-office-location']
});

// Replace this with your real tests.
test('it exists', function() {
  var controller = this.subject();
  ok(controller);
});
