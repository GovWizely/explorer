import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('controller:eccn-entries/index', 'EccnEntriesIndexController', {
  // Specify the other units that are required for this test.
  needs: ['controller:eccn-entries']
});

// Replace this with your real tests.
test('it exists', function() {
  var controller = this.subject();
  ok(controller);
});
